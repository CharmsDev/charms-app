version: 11

tx:
  ins:
    - ${in_utxo_0}
  outs:
    - 0:
        ticker: {{project-name | upcase}}
        remaining: 100000
  coins:
    - amount: ${amount_0}
      dest: ${dest_0}

app_public_inputs:
  n/${app_id}/${app_vk}: ~
