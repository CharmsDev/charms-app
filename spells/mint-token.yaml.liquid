version: 11

tx:
  ins:
    - ${in_utxo_1}
  outs:
    - 1: 69420
    - 0:
        ticker: {{project-name | upcase}}
        remaining: 30580
  coins:
    - amount: ${amount_1}
      dest: ${dest_1}
    - amount: ${amount_2}
      dest: ${dest_2}

app_public_inputs:
  n/${app_id}/${app_vk}: ~
  t/${app_id}/${app_vk}: ~
